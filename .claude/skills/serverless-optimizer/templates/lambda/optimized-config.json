{
  "description": "Optimized Lambda function configuration template",
  "configurations": {
    "memory-optimized": {
      "memorySize": 512,
      "timeout": 30,
      "reservedConcurrentExecutions": null,
      "provisionedConcurrencyConfig": null,
      "environment": {
        "variables": {
          "NODE_ENV": "production",
          "LOG_LEVEL": "info"
        }
      },
      "layers": [],
      "description": "General-purpose optimized configuration for most workloads"
    },
    "high-performance": {
      "memorySize": 1024,
      "timeout": 60,
      "reservedConcurrentExecutions": 10,
      "provisionedConcurrencyConfig": {
        "provisionedConcurrentExecutions": 5
      },
      "environment": {
        "variables": {
          "NODE_ENV": "production",
          "LOG_LEVEL": "warn"
        }
      },
      "layers": [],
      "description": "High-performance configuration for latency-sensitive workloads"
    },
    "cost-optimized": {
      "memorySize": 256,
      "timeout": 15,
      "reservedConcurrentExecutions": null,
      "provisionedConcurrencyConfig": null,
      "environment": {
        "variables": {
          "NODE_ENV": "production",
          "LOG_LEVEL": "error"
        }
      },
      "layers": [],
      "description": "Cost-optimized configuration for non-critical workloads"
    },
    "java-optimized": {
      "memorySize": 2048,
      "timeout": 120,
      "reservedConcurrentExecutions": null,
      "provisionedConcurrencyConfig": {
        "provisionedConcurrentExecutions": 2
      },
      "environment": {
        "variables": {
          "JAVA_TOOL_OPTIONS": "-XX:+TieredCompilation -XX:TieredStopAtLevel=1"
        }
      },
      "layers": [],
      "description": "Optimized for Java runtime to reduce cold starts"
    }
  },
  "bestPractices": {
    "coldStartReduction": [
      "Use Provisioned Concurrency for latency-sensitive functions",
      "Minimize package size by removing unused dependencies",
      "Use Lambda Layers for common dependencies",
      "Prefer interpreted languages (Node.js, Python) over compiled (Java, .NET)",
      "Increase memory allocation for faster CPU (cost vs performance trade-off)"
    ],
    "costOptimization": [
      "Right-size memory based on actual usage",
      "Use ARM (Graviton2) for 20% cost savings",
      "Implement request batching where possible",
      "Set appropriate timeout to avoid wasted execution time",
      "Monitor and optimize execution duration"
    ],
    "performance": [
      "Initialize SDK clients outside the handler",
      "Use connection pooling for databases",
      "Enable X-Ray for performance insights",
      "Implement caching strategies",
      "Use asynchronous operations where possible"
    ]
  }
}
